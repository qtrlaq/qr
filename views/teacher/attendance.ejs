<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Список посещаемости</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <div class="container">
        <h1>Сессия #<%= sessionRow.id %></h1>
        <a class="btn" href="/teacher">Назад</a>
        <ul>
            <% rows.forEach(r => { %>
                <li><%= r.email %> — <%= r.marked_at %></li>
            <% }) %>
        </ul>
    </div>
    
</body>
</html>


